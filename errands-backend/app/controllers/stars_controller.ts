import User from '#models/user'

export default class StarsController {
public async store({response,request,auth,params}){
  // check iflogged in
  // check if loggedin user is the writing
  // validate request
  //save
}

}
